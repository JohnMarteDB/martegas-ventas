# Only the public dashboard (docs/) is published. The full detailed dataset and
# parse cache stay local/private (payment-level detail + source refs).
# NOTE: leading slash anchors to repo root, so docs/data/ (the published
# aggregates) is NOT ignored — only the top-level data/ folder is.
/data/
__pycache__/
*.pyc
.DS_Store
desktop.ini
